# Expo HAS CHANGED

Dieses Projekt ist bewusst auf Expo SDK 54 gepinnt (siehe CLAUDE.md) —
NICHT auf eine neuere Version upgraden, da Expo Go auf den Handys nur mit SDK 54 funktioniert.

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
